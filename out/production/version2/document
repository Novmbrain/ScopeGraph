1. 查找多条路径，包括找不到的path(ok)
2. 将查找代码object化(ok)

4. interface pour path(os)
5. 给测试加上commentaire(ok)

----------------------------
6. Graphviz / DOT
7. fusioner les scopes
8. rapport(decision, le reste, )

---
1. create a classe for pathImpl(ok)
2. try abstract in Edge

4. 扩展关于module的能寻找多个declaration(ok)
5. 创建一个类专门用于生成测试的scope graph(os)
6. 自动生成graphiz的代码(os)
7. rapport

-------------------
1. commencer le rapport
2. copier, fusion(cas marche et march pas) + compiler
3. printDot reconstruct enlever if() / distribuer printDot dans les sub-classes.


---------
问题
1. copy的代码没有很好的遵守面对对象
2. scope必须要用名字来进行区分(一开始建模遗留下来的问题)


1. 在copy的时候确认一下是新的，index + 1000000
2.


